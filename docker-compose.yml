version: '3'
services:
  redis:
    image: "redis:latest"
    ports:
      - 6379:6379
    expose: 
      - 6379
  # app:
  #   ports:
  #     - 3000:3000
  #   build: ./
  #   volumes:
  #     - ./:/var/www/app
  #   environment:
  #     - NODE_ENV=development
  #     - PORT=3000
  #   command:
  #     sh -c 'npm i && node server.js'